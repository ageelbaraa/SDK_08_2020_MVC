
@using DataAccressProject.Models;

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div style="font-family:Arial">
    <h2>
        Employee List
    </h2>

    <ul>
        @foreach (Employee_1 item in Model)
        {
            <li>
                @Html.ActionLink(item.name, "Details", new { id = item.Id })
            </li>
        }
    </ul>

</div>

